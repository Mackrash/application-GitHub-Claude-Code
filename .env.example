# === Notion Configuration ===
# Crée une intégration sur https://www.notion.so/my-integrations
# Puis partage tes pages/databases avec l'intégration
NOTION_API_KEY=ntn_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# === MCP Server Configuration ===
# Transport: "stdio" (pour Claude Desktop) ou "sse" (pour n8n)
MCP_TRANSPORT=sse

# Port du serveur SSE (utilisé quand MCP_TRANSPORT=sse)
MCP_PORT=3001

# === n8n Configuration (Docker) ===
N8N_PORT=5678
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=changeme
